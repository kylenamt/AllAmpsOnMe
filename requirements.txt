# Phase 1 — TONE3000 acquisition runtime dependencies.
# Pinned per spec §2. Tested on Python 3.10–3.13, Linux, CPU-only.

# --- HTTP / API client ---
requests==2.32.3

# --- Data / manifests ---
numpy==1.26.4
pandas==2.2.3
pyarrow==18.1.0
soundfile==0.12.1

# --- CLI / UX ---
click==8.1.7
tqdm==4.67.1
python-dotenv==1.0.1

# --- NAM validation stack (used only by `t3k validate`) ---
# neural-amp-modeler pulls in torch + pytorch-lightning.
# SUPPLY-CHAIN NOTE (spec §2): pytorch-lightning 2.6.2 and 2.6.3 were subject to a
# known supply-chain incident. We pin a safe, earlier release and cap below 2.6.2.
neural-amp-modeler==0.11.0
torch==2.5.1
pytorch-lightning==2.5.2
